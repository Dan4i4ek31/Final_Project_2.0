# Добавление обложек книг 📚

## Что было изменено

Проведено обновление проекта для добавления обложек ко всем 50 книгам в базе данных.

### 1. Обновлена модель BooksModel (`app/models/books.py`)

**Добавлено новое поле:**
```python
cover_image: Mapped[Optional[str]] = mapped_column(String(500), nullable=True)
```

Это поле содержит URL изображения обложки книги.

### 2. Обновлен скрипт заполнения БД (`populate_db.py`)

**Добавлены URL обложек для всех 50 книг:**
- Классическая литература (Война и мир, Преступление и наказание и т.д.)
- Зарубежная литература (Шерлок Холмс, Три мушкетера и т.д.)
- Научная фантастика (1984, Машина времени и т.д.)
- Детская литература (Гарри Поттер, Алиса в стране чудес и т.д.)
- И многие другие жанры

Все URL обложек берутся с надежного источника (Goodreads API).

### 3. Создана миграция БД (`migrations/versions/001_add_cover_image_to_books.py`)

Миграция добавляет новую колонку `cover_image` в таблицу `books`.

## Как использовать

### Способ 1: Пересоздать БД с нуля (рекомендуется)

```bash
# 1. Удалите существующий файл БД
rm foliant.db

# 2. Запустите скрипт заполнения
python populate_db.py
```

### Способ 2: Применить миграцию к существующей БД

```bash
# 1. Примените миграцию
alembic upgrade head

# 2. Обновите данные о книгах (опционально)
# Вы можете написать скрипт для обновления существующих записей
```

## Использование обложек в приложении

Теперь вы можете отобразить обложку книги в шаблонах:

```html
<!-- Пример в HTML шаблоне -->
<div class="book-card">
    {% if book.cover_image %}
        <img src="{{ book.cover_image }}" alt="{{ book.title }}" class="book-cover">
    {% else %}
        <img src="/static/placeholder-cover.png" alt="No cover" class="book-cover">
    {% endif %}
    <h3>{{ book.title }}</h3>
    <p>{{ book.author.name }}</p>
</div>
```

## Пример API ответа

```json
{
    "id": 1,
    "title": "Война и мир",
    "description": "Эпическое полотно жизни и смерти, войны и мира.",
    "year": 1869,
    "cover_image": "https://images.gr-assets.com/books/1462971869l/656.jpg",
    "author": {
        "id": 1,
        "name": "Лев Толстой"
    },
    "genre": {
        "id": 1,
        "name": "Роман"
    }
}
```

## Данные о книгах

Все 50 книг с обложками включают:
- Классику русской литературы
- Зарубежную классику
- Современную литературу
- Научную фантастику
- Детскую литературу
- И другие жанры

Каждая книга содержит:
- Название
- Автора
- Жанр
- Год издания
- Описание
- **URL обложки** (новое!)

## Структура БД

```
Таблица: books
├── id (PRIMARY KEY)
├── title
├── description
├── year
├── author_id (FOREIGN KEY)
├── genre_id (FOREIGN KEY)
└── cover_image (NEW!) ← Новое поле для URL обложки
```

## Примечания

- ✅ Все 50 книг имеют обложки
- ✅ URL обложек указывают на внешний ресурс (Goodreads)
- ✅ Поле nullable, поэтому старые записи не будут нарушены
- ✅ Можно использовать placeholder изображение при отсутствии обложки
- ✅ Миграция обратима (можно откатить через `alembic downgrade`)

## Команды для запуска

```bash
# Установить зависимости
pip install -r requirements.txt

# Создать/обновить БД
python populate_db.py

# Запустить приложение
python main.py

# Применить миграции (если нужно)
alembic upgrade head
```

Приятного использования! 📖✨
