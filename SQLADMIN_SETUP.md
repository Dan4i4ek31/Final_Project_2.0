# SQLAdmin Integration Guide

Этот документ описывает интеграцию SQLAdmin в ваш проект Library Management API.

## Что такое SQLAdmin?

SQLAdmin — это экстенсивная, раскрасиваемая admin-панель для FastAPI, основанная на SQLAlchemy.

### Ключевые возможности:
- Автоматически генерируемая CRUD интерфейс для всех моделей
- Поддержка асинхронных операций
- Кустомизация винтью моделей
- Отчеты и аналитика
- Поддержка многоязычности

## Что было истючено

### 1. Добавлено в `requirements.txt`:
```
sqladmin==0.19.1
```

### 2. Создан `app/admin.py`:
- С классами ModelView для каждой модели:
  - **RoleAdmin** - управление ролями
  - **UserAdmin** - управление пользователями
  - **AuthorsAdmin** - управление авторами
  - **GengresAdmin** - управление жанрами
  - **BooksAdmin** - управление книгами
  - **BookCommentsAdmin** - управление комментариями
  - **ShelfAdmin** - управление полками

### 3. Создан `app/database/async_db.py`:
- Async engine для SQLite используя aiosqlite
- Async session factory
- Конвертация DATABASE_URL в async формат

### 4. Обновлен `main.py`:
- Импортирован setup_admin и async_engine
- Инициализация admin-панели в аппликации

## Как использовать

### Установка зависимостей:
```bash
pip install -r requirements.txt
# или если вы используете uv:
uv sync
```

### Запуск приложения:
```bash
python main.py
# или
# uvicorn main:app --reload
```

### Доступ к admin-панели:
Админ-панель доступна в веб-браузере по одному из адресов:
- http://localhost:8000/admin
- http://127.0.0.1:8000/admin

## Основные фичи

### Доступные модели в admin:

1. **Роли** (Роль)
   - Просмотр, недавно сохраненные роли
   - Добавление, редактирование, удаление ролей

2. **Пользователи** (Пользователь)
   - Вывод таблицы всех пользователей
   - Отсутствиют сенситивные поля (пароль)
   - Статус активности показывается евросимволом (✓/✗)

3. **Авторы** (Автор)
   - Описание подробных данных автора

4. **Жанры** (Жанр)
   - Полные описания генров

5. **Книги** (Книга)
   - Основная информация (описание не выводится в списке)
   - Рейтинг год публикации

6. **Комментарии** (Комментарий)
   - Комментарии на книги с рейтингом
   - Наручная модерация контента

7. **Полки** (Полка)
   - Организация начало чтения книг
   - Статус чтения книг

## Настройка

### Прибавление автентификации:

В `app/admin.py`, в функции `setup_admin()`:

```python
# Цар аутентификация
from sqladmin.authentication import AuthenticationBackend

class AdminAuth(AuthenticationBackend):
    async def login(self, request: Request):
        # Логика автентификации
        pass
    
    async def logout(self, request: Request):
        pass

admin = Admin(
    app=app,
    engine=engine,
    authentication_backend=AdminAuth(),  # добавить сюда
)
```

### Отправка операций:

Dje добавить поле в admin:

```python
class UserAdmin(ModelView, model=UserModel):
    # ...
    column_editable_list = [UserModel.full_name, UserModel.email]
    column_filters = [UserModel.is_active, UserModel.role_id]
```

## Полезные ресурсы

- [SQLAdmin Documentation](https://aminalaee.dev/sqladmin/)
- [SQLAlchemy ORM](https://docs.sqlalchemy.org/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)

## Троблшутинг

### Ошибка: "Не подвязывается async engine"

Гвайнесь, тчто Вы используете `app/database/async_db.py`, а не синхронные движок. SQLAdmin требует AsyncEngine.

### Ошибка: "Не могу сохранить данные"

Проверьте права доступа к базе данных и текущее ссоединение (при использовании SQLite, убедитесь, что foliant.db присутствует и данные хорошо).

## Направленные обновления

В будущем, можно добавить:
- Отсутствие в admin
- Настроённые выявленные аналитики
- Ихпорт/Наюлус данных
- Отчёты

---

Наслаждайтесь вашей новой admin-панелью! 🐧
